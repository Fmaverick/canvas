- [x] 主体库图片素材可触发同步到火山素材库，并能创建或复用对应的 `Asset Group`
- [x] 本地素材记录可保存火山 `Asset ID`、`Asset Group ID`、同步状态、最近同步时间与失败信息
- [x] 主体详情或素材列表接口会返回火山同步摘要字段，前端可直接展示
- [x] `CreateAsset` 异步流程已被处理，只有 `GetAsset` 返回 `Active` 后素材才被标记为可引用
- [x] 视频节点引用主体库素材时，已同步素材优先转为 `asset://<volcengine_asset_id>`
- [x] 未同步、处理中或失败素材会自动回退到现有公网 URL，不阻断视频生成
- [x] Seedance2.0 视频请求构造与校验支持 `asset://` URI，不会误判为非法 URL
- [x] 缺少火山配置、项目不一致、远端失败、重复同步四类场景具有明确结果或错误反馈

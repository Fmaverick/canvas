- [x] `input` 与 `combination` 节点协议、连线语义和运行态摘要已按 spec 落地
- [x] 数据库已支持输入项、组合计划、组合实例和分片执行，并保留旧批量模式兼容
- [x] 组合计划支持估算、预览、运行、暂停、恢复、取消和详情查询
- [x] 生成节点已能按组合实例逐条执行，而不是直接对多个输入节点做隐式展开
- [x] 大规模组合已通过分片调度、容量检查、工作空间配额和供应商熔断治理推进，不会一次性创建全部任务
- [x] `batch_result` 已能按组合实例展示结果、失败信息和输入绑定摘要
- [x] 单个组合实例支持重试、提取和追溯到输入绑定与任务记录
- [x] 运行日志和指标已覆盖 `plan_id`、`shard_id`、`combination_item_id`、`node_run_id`、`task_id`
- [x] 关键验证已覆盖估算、组合展开、治理阈值、调度容量、供应商熔断与类型检查，并完成本轮兼容路径回归
